{
  "name": "tf2cost",
  "version": "1.1.1",
  "description": "A module for parsing, formatting and converting TF2 currencies.",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src --dts --format esm,cjs --clean",
    "build:watch": "pnpm run build --watch",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "tf2",
    "trading",
    "currency",
    "parse",
    "format",
    "convert"
  ],
  "author": "prenaissance",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "@vitest/coverage-c8": "^0.30.1",
    "tsconfig": "workspace:*",
    "tsup": "^6.7.0",
    "typescript": "^5.0.4",
    "vite-tsconfig-paths": "^4.2.0",
    "vitest": "^0.30.1"
  },
  "dependencies": {
    "arcsecond": "^5.0.0"
  }
}
