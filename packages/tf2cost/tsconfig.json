{
  "extends": "tsconfig/base.json",
  "include": ["**/*.d.ts", "src", "tests"],
  "exclude": ["node_modules", "dist", "coverage", "tests/fixtures"],
  "compilerOptions": {
    "outDir": "dist",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
